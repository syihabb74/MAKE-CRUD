/** @type {import('next').NextConfig} */
const nextConfig = {
    images: {
        domains: ['img.freepik.com', 'facebook.com', "getavataaars.com", 'avataaars.io']
    }
}

module.exports = nextConfig
